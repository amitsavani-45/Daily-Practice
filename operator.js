 //Special symbol use dto perform operation on values 
 /*
 //Artihmetic operators 

 //addition 
 let a=10;
 let b=20;
 let c=Number(a)+Number(b);
 console.log("Addition is ",c);

  //subtraction 
 let d=30;
 let e=7;
 let f=d-e;
 console.log("SUbtraction is ",f);

 //multiplication
 let g=30;
 let h=7;
 let i=g*h;
 console.log("multiplication is ",i);

 //Division 
 let x=36;
 let y=9;
 let z=x/y;
 console.log("Division is ",z);

 //Modulus
 let A=40;
 let B=10;
 let C=A%B;
 console.log("Modulus is",C);

 //Assignment operators 

 let X=5;
 let name="alice";

let count = 10;
count += 5;  // count is now 15
count *= 2;  // count is now 30

//Comparison Operator 

// Equal to (==) - compares values, with type coercion
5 == "5"       // true (string "5" is converted to number)
10 == 10       // true

// Strict equal to (===) - compares values AND types
5 === "5"      // false (different types)
5 === 5        // true

// Not equal to (!=) - with type coercion
5 != "6"       // true
5 != "5"       // false

// Strict not equal to (!==) - compares values AND types
5 !== "5"      // true (different types)
5 !== 5        // false

// Greater than (>)
10 > 5         // true
3 > 8          // false

// Less than (<)
5 < 10         // true
15 < 10        // false

// Greater than or equal to (>=)
10 >= 10       // true
10 >= 5        // true
3 >= 8         // false

// Less than or equal to (<=)
5 <= 10        // true
10 <= 10       // true
15 <= 10       // false

//Logical operator 

// Logical And && 

let age = 25;
let hasLicense = true;

// Both conditions must be true
age >= 18 && hasLicense  // true

age >= 18 && !hasLicense // false (second condition is false)

// Example in if statement
if (age >= 18 && hasLicense) {
    console.log("You can drive");
}

// Logical or ||

let isWeekend = false;
let isHoliday = true;

// At least one condition must be true
isWeekend || isHoliday   // true

isWeekend || false       // false (both are false)

// Example
if (isWeekend || isHoliday) {
    console.log("No work today!");
}

//Logical Not !

let isRaining = false;

!isRaining              // true (opposite of false)
!true                   // false

// Example
if (!isRaining) {
    console.log("Let's go outside!");
}
*/
/*
let a = 10;
let b = 3;

console.log(a + b);  
console.log(a - b);  
console.log(a * b);  
console.log(a / b); 
console.log(a % b);  
console.log(a ** b); 

let x = 5;
x += 3;  
x -= 2;  
x *= 4;  
x /= 3;  
x %= 5;  

let p = 5;
let q = "5";

console.log(p == q);   
console.log(p === q);  
console.log(p != q);   
console.log(p !== q);  
console.log(p > 3);    
console.log(p < 10);  
console.log(p >= 5);   
console.log(p <= 4);   

let age = 20;
let status = (age >= 18) ? "Adult" : "Minor";
console.log(status);  

*/

let a=5;
x=a%2;
console.log(x);


let b=7;
let y;
y=++b;
console.log(y);